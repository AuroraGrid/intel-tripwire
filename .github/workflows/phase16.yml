name: AURORA Phase 16 CI

on:
  pull_request:
    paths:
      - "aurora-live/phase16_*.py"
      - "aurora-live/release_wsgi.py"
      - "aurora-live/tests/test_phase16.py"
      - ".github/workflows/phase16.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -r aurora-live/requirements.txt
      - run: python -m py_compile aurora-live/phase16_detection.py aurora-live/phase16_complete.py aurora-live/release_wsgi.py
      - run: python -m unittest discover -s aurora-live/tests -p "test_phase16.py" -v
